# foodly.solutions — Foodly's marketing landing page.
#
# This is the public face of the product (positioning, features, app
# store links). All content is intentionally indexable in EN (/),
# ES (/es) and PT-PT (/pt); we WANT search engines to surface it for
# queries like "Foodly", "menu digital QR restaurante", "carta digital
# restaurante", "digitalize my restaurant", etc.
#
# /join is a functional group-order invitation page (excluded from
# indexing on-page) — keep it out of crawl budget entirely.
#
# The sitemap below is the canonical source of indexable URLs. Keep it
# in sync when adding new pages.

User-agent: *
Allow: /
Disallow: /join

Sitemap: https://foodly.solutions/sitemap.xml
